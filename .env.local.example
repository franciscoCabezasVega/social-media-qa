# Ejemplo de configuración de .env.local
# Copia este archivo a .env.local y llena los valores

# ==================== DESARROLLO ====================
# Usar JSON local para pruebas
DB_MODE=development

# ==================== VARIABLES VERCEL KV (Producción) ====================
# Dejar vacío en desarrollo, llenar en producción
# Copiar desde: Vercel Dashboard → Upstash Redis
KV_URL=
KV_REST_API_URL=
KV_REST_API_TOKEN=

# ==================== VARIABLES VERCEL BLOB (Opcional) ====================
# Para subir imágenes a Vercel Blob en producción
BLOB_READ_WRITE_TOKEN=

# ==================== JWT SECRET ====================
# Cambiar este valor por uno seguro
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_key_change_in_production_12345

# ==================== NODE ENVIRONMENT ====================
NODE_ENV=development
