# Ejemplo de configuración de .env.local
# Copia este archivo a .env.local y llena los valores

# ==================== DATABASE - POSTGRESQL ====================
# Copiar desde Vercel Storage → Neon Database
# DATABASE_URL: Connection string estándar (con pgBouncer pooler)
# DATABASE_URL_UNPOOLED: Sin pgBouncer (para migraciones)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require
DATABASE_URL_UNPOOLED=postgresql://user:password@host/database?sslmode=require

# ==================== VERCEL BLOB (Para imágenes) ====================
# Copiar desde Vercel Storage → Blob
BLOB_READ_WRITE_TOKEN=vercel_blob_xxx...

# ==================== JWT SECRET ====================
# Cambiar este valor por uno seguro
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_super_secret_key_change_in_production_12345

# ==================== NODE ENVIRONMENT ====================
NODE_ENV=development
